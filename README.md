# Merge Flowers — Play Online Free

Static product repository generated from the main `gamesite` Hermes system.

- Slug: `merge-flowers`
- Domain: https://flowermerger.site
- Canonical URL: https://flowermerger.site/game/merge-flowers
- Render mode: iframe

## Structure

- `/game/merge-flowers/` contains the playable landing page.
- `/privacy`, `/terms`, `/cookies`, and `/disclaimer` are static compliance pages.
- `CNAME`, `robots.txt`, and `sitemap.xml` are ready for static hosting.

## Deploy

Push this folder to its own GitHub repository and enable GitHub Pages or connect it to a static host.
